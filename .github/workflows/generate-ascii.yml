name: generat ascii

on: 
  push

jobs:
  ascii_job:
    runs-on: ubuntu-latest
    steps:
    - name: checkout repo
      uses: actions/checkout@v4

    - name: list
      run: ls -latr  

    - name: excuting shell
      run: |
        chmod +x ascii.sh
        ./ascii.sh

        